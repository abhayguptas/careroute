version: '3.8'

services:
  careroute:
    build: .
    container_name: careroute-app
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
